API_TOKEN=changeme
PORT=4000
# Cron: second minute hour day-of-month month day-of-week (node-cron format)
CRON_SPEC=0 2 * * *
# Set to true to disable cron scheduling (manual ingest only)
DISABLE_CRON=false
# SerpApi (preferred) or Serper keys for trends
SERPAPI_KEY=
SERPER_API_KEY=
# Apify fallback (optional)
APIFY_TOKEN=
APIFY_TASK=
